"""MCP server entry point. Filled out in PR 6; for now we surface a clear error."""

from __future__ import annotations


def run() -> None:
    raise NotImplementedError(
        "MCP server is scheduled for PR 6 of the Phase 1 plan. "
        "Use the `weighted-evidence` CLI for now."
    )
